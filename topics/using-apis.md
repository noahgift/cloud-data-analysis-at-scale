## Using APIS

One particularly cloud native item is to use APIS.

### Create GCP GAE web service with APIS

This screencast shows how to create a Google App Engine service and auto-deploy an web service that uses Cloud APIS.